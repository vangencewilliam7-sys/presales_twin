# Presales Digital Twin Frontend
> Person 4 owns this folder. Initialize with React + Vite here.

## Setup (Person 4)
```bash
cd frontend
npx -y create-vite@latest ./ -- --template react-ts
npm install
npm install react-router-dom axios mermaid recharts react-markdown lucide-react
npm run dev
```
